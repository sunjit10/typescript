// public and private is TS specific additionsclass Person {   // Default is public, use it to explicitly communicate that   // it can be changed from outside   public fname: string;   lname: string;      private age: number;   // If ECMAScript 2015 or higher, you can do #age vs private age      constructor(fname:string, lname:string) {     this.fname = fname;     this.lname = lname;     this.age = 30;   }   public get fullName() {     return `${this.fname}  ${this.lname}`;   }   public set newLastName(newLastName) {     this.lname = newLastName;   }   private set newFirstName(newFirstName) {      this.fname = newFirstName;   }}let person1: Person = new Person("James", "Smith");console.log(person1);// cannot access a private variable from outside//person1.age = 17;person1.newLastName = "Adams";// Cannot access a private method//person1.newFirstName = "Jim";console.log(person1.fullName);